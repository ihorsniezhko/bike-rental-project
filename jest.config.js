module.exports = {
  // The test environment that will be used for testing
  testEnvironment: 'jsdom',
  // A path to a module which exports a function to be triggered once per test file
  setupFilesAfterEnv: ['<rootDir>/setupTests.js'],
};